# 在安卓手机上跑 Pumpkin（壳 App 方式）

本文说明如何用 **Pumpkin 壳 App** 在安卓手机上部署运行 Pumpkin（Rust 编写的 Minecraft 服务端）。壳本身很轻（APK 约 35KB），只负责下载、启动服务端二进制并显示控制台，内部**不含服务端**。

> 仓库：独立新仓库 <https://github.com/a776058959/Pumpkin_build>（不是 fork，只放壳源码与构建流水线，不保存上游代码）。

---

## 概述

- 壳 APK 仅约 **35KB**，里面**没有服务端**，服务端二进制是运行时从 GitHub Releases 下载的。
- `AndroidManifest` 里 `targetSdk=28`，这是本方案能直接执行私有目录内程序的关键（原理见文末）。
- Release 附件分两类：
  - `pumpkin-android-arm64-<日期>`——服务端原生二进制，App 下载并运行它；
  - `pumpkin-shell-<日期>.apk`——壳安装包。

---

## 一、安装壳 App

1. 到 Release 下载 `pumpkin-shell-<日期>.apk`，传到手机（USB / 网盘 / 微信 / QQ 均可），点击安装。
2. 系统提示时允许「安装未知应用」（不同品牌入口略有差异，一般会弹窗引导，或到「设置 → 应用/安全」开启）。
3. 只支持 **arm64（64 位 ARM）**；32 位老手机、x86 模拟器装不了。

---

## 二、界面说明与首次使用

App 界面为**三个页面**，用底部**悬浮导航栏**切换。整体是深色渐变背景，卡片和导航栏都是**半透明玻璃质感**——导航栏背后是真实的内容模糊（Android 12 及以上用硬件模糊，更低版本用软件模糊），选中项用主题色高亮。

### 页面一「运行」

- **服务器状态**：圆点 + 运行时长。
- **局域网联机地址**：形如 **Java `192.168.1.7:25565`** 和 **基岩 `192.168.1.7:19132`**。
- **数据目录**、**启动 / 停止**按钮、**电池优化**按钮、**复制地址**按钮。
- **控制台**：实时日志 + 命令输入框，支持 `list`、`op 玩家名`、`save-all`、`stop` 等命令。

### 页面二「更新」

- 当前版本号、已安装版本数量与磁盘占用。
- **检查更新**按钮、**下载或更新**按钮（带进度条）、**切换或回滚**按钮、**删除版本**按钮。
- 页面下方有**说明卡片**，解释数据存放位置与保留策略。

### 页面三「设置」

- **启动方式切换**：普通模式 / Root 模式。
- **下载源设置**：GitHub API 地址、仓库 owner/repo、下载镜像前缀三个输入框 + 保存按钮。下载源在此**显式可改**，不再需要长按。手机直连 GitHub 不通时，把 API 地址换成能返回同样 JSON 的镜像，并把下载镜像前缀填成例如 `https://ghfast.top/`（会拼在 `https://github.com/...` 前面）。
- **清理数据**：清服务端版本 / 清游戏数据 / 全部清空三种粒度。
- **关于**：壳版本号、服务端机器、电池优化设置、复制数据目录路径。

首次使用按顺序操作：

1. 到「更新」页**检查更新**——读取 GitHub Releases，找出 `pumpkin-android-arm64-<日期>` 附件。
2. **下载最新版本**——下载到应用内部私有目录 `versions/<标签名>/pumpkin`。
3. **启动**——直接运行下载好的服务端。

- 服务以**前台服务**运行，切后台或锁屏也能继续（通知栏有常驻通知）。
- 界面会直接显示联机地址，例如 **Java `192.168.1.7:25565`** 和 **基岩 `192.168.1.7:19132`**。

---

## 三、两种启动模式

界面上有「**启动方式**」按钮切换，两种模式：

### 普通模式（默认）

- App 直接启动下载到**私有目录**的服务端二进制。之所以能执行，是因为 `targetSdk<=28` 的应用被放进 `untrusted_app_27` 这个 SELinux 域，该域**允许对应用私有目录里的文件执行 execve**；`targetSdk>=29` 会被拒绝（即所谓 **W^X 限制**）。这是默认且最省事的方式。

### Root 模式

- 用 `su -c` 命令启动，进程落在 **magisk/su 域**，该域本身不受私有目录执行限制。
- **完全不改动 SELinux**：保持 Enforcing、不改策略文件，所以不会被那些检测 `getenforce` 或 SELinux 状态的 App 发现。
- 首次使用会弹 **Magisk 授权框**。
- 额外价值：**如果将来 Android 取消旧 targetSdk 的豁免，Root 模式仍然可用**。

| 对比项 | 普通模式 | Root 模式 |
|---|---|---|
| 是否需要 Root | 否 | 是（Magisk） |
| 原理 | 靠 targetSdk≤28 的域豁免 | 进程在 magisk/su 域，不受私有目录执行限制 |
| 是否改 SELinux | 不改（本就豁免） | 不改（保持 Enforcing、不改策略） |
| 授权弹窗 | 无 | 首次有 Magisk 授权框 |
| 抗性 | 依赖旧 targetSdk 豁免 | 豁免取消后仍可用 |

---

## 四、数据目录与文件管理

数据分**两处**：

- **服务端程序（各版本）**：应用内部私有目录 `versions/<标签名>/`。**必须如此**，否则无法对该目录执行 execve。普通用户无法直接访问，管理就走 App 内功能。
- **游戏数据**：外部目录 `/sdcard/Android/data/com.pumpkin.server/files/server/`，内含 `config/`、`world/`、`logs/`。

游戏数据可以这样管理：

- 用 **USB 连接电脑**，或手机自带的「文件」App 进入 `Android/data/com.pumpkin.server/files/server/`；
- 改造配置、放入现成的世界存档，都在这里。

---

## 五、版本管理与回滚

- 当前版本之外**最多保留 3 个历史备份**。
- **切换版本**可以回滚到之前的备份；**检查更新 + 下载最新版本**就是升级。
- 注意：切换版本前**要先停止服务端**（会提示），否则文件占用可能失败。

---

## 六、清理数据

「清理数据」按钮有三种粒度，**先看清会删什么再点**：

| 选项 | 清掉的 | 保留的 |
|---|---|---|
| 清理服务端版本 | 下载的服务端程序 | 游戏数据 |
| 清理游戏数据 | `config/ world/ logs/` | 服务端版本 |
| 全部清理 | 两者都删 | 无 |

---

## 七、联机与性能调优

- 同一局域网内，Java 版连 `手机IP:25565`，基岩版连 `手机IP:19132`（地址直接在界面上显示）。
- 性能通用的建议（不逐项照搬可能过时的字段名，**以 App 实际生成的配置文件为准**）：
  - 调小**视距**、调小**模拟距离**，大幅降低区块生成与实体模拟开销，省内存省电；
  - 控制**玩家人数**，手机不适合承载高并发；
  - 必要时用**离线模式**（`online_mode=false`），绕开联网认证。
- 长时间满载会发热降频、吞吐下降，建议按手机内存（建议 ≥4GB）平衡以上参数。

---

## 八、常见问题排查

| 现象 | 原因 / 解决 |
|---|---|
| 下载进度不动 | 手机访问 GitHub 不通（网络被墙/受限）。挂代理、换网络（如手机热点 / 电脑热点给手机），或用电脑下载后手动放入对应位置 |
| 启动后日志出现 `Permission denied` | 普通模式的域豁免未生效（或设备情况特殊）。**改用 Root 模式**启动即可 |
| 进程被系统杀掉 | 手机开了**电池优化**。到系统设置把本 App（或壳）设为「不受限制」，并尽量保持在前台 |
| 切换版本提示要先停止 | 服务端仍占用文件。先停止服务端再切换版本 |
| 清理会删什么 | 见「清理数据」表——三种粒度各不相同，**点之前确认** |

---

## 九、备选方式：Termux

- 旧文档里的 **Termux 方式仍然有效**，作为备选保留：用 F-Droid 版 Termux，把 `pumpkin-android-arm64-<日期>` 二进制放到 Termux 私有目录（`~/`），对 `/sdcard` 或 `~/storage`（noexec 挂载）一律不行。
- 运行前 `termux-wake-lock`、关电池优化，尽量在前台会话运行。
- **原理与壳一样**：Termux 能运行二进制，靠的同样是 `targetSdk<=28` 应用的 SELinux 域豁免，**不是 Termux 有特权**。Android 10+ 上普通模式能执行私有目录程序，和 Termux 是同一机制。

---

## 十、原理说明

- Android 10+ 只有 **`targetSdk<=28`** 的应用会被放进 `untrusted_app_27` SELinux 域，该域**允许对应用私有目录执行 execve**；`targetSdk>=29` 落在更受限的域，被 W^X 限制，不能从私有目录执行程序。
- 壳因此把下载的服务端放在**私有目录**执行（普通模式即此机制）。
- Root 模式进程落在 magisk/su 域，**不触碰 SELinux 策略**，故 `getenforce` 等检测仍显示 Enforcing，不会被发现；并且未来即使取消旧 targetSdk 豁免，Root 模式也依然可用。
- Release 保留策略：**<1 个月全部保留；1–2 个月每周保留最后一个；2–12 个月每月保留最后一个；>1 年删除**。
