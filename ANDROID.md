# 在安卓手机上跑 Pumpkin（南瓜坞 App 方式）

本文说明如何用 **南瓜坞 App** 在安卓手机上部署运行 Pumpkin（Rust 编写的 Minecraft 服务端）。南瓜坞本身只负责下载、启动服务端二进制并显示控制台，内部**不含服务端**。

> 仓库：独立新仓库 <https://github.com/a776058959/Pumpkin_build>（不是 fork，只放 App 源码与构建流水线，不保存上游代码）。
>
> **本项目（除上游服务端外）由 AI 编写**，详见 [README](README.md#关于本项目代码由-ai-编写)。

---

## 概述

- 南瓜坞 APK 里**没有服务端**，服务端二进制是运行时从 GitHub Releases 下载的。
- APK 体积约 **14MB**。界面从早期的手写 Java View 换成 **Compose + miuix** 之后变大了
  （那之前约 90KB），换来的是 miuix 风格的控件、液态玻璃底栏和可切换的多套配色。
- `AndroidManifest` 里 `targetSdk=28`，这是本方案能直接执行私有目录内程序的关键（原理见文末）。
- Release 附件分两类：
  - `pumpkin-android-arm64-<日期>`——服务端原生二进制（约 119MB），App 下载并运行它；
  - `pumpkin-shell.apk`——安装包（约 90KB）。

---

## 一、安装南瓜坞 App

1. 去 [Releases](https://github.com/a776058959/Pumpkin_build/releases) 页，找**最新的那个带
   `pumpkin-shell.apk` 附件的发布**，下载这个 APK。
   > ⚠️ 别再依赖 `releases/latest/download/pumpkin-shell.apk` 这个"永久链接"了。
   > 服务端与 App 现在是**两条独立流水线**，服务端发布更频繁，`latest` 通常是一个
   > **不带 APK** 的服务端发布，那个链接会 404。直接看 Releases 页更稳。
2. 下载 APK 传到手机（USB / 网盘 / 微信 / QQ 均可），点击安装。
3. 系统提示时允许「安装未知应用」（不同品牌入口略有差异，一般会弹窗引导，或到「设置 → 应用/安全」开启）。
4. 只支持 **arm64（64 位 ARM）**；32 位老手机、x86 模拟器装不了。
5. 装好之后**不用再手动下载**：App 里「设置 → 检查更新」会在应用内下载新版并直接调起系统安装器。

---

## 二、界面说明与首次使用

App 有**应用图标**（蓝紫渐变底 + 白色 Pumpkin 字母 P，自适应图标，Android 8.0 及以上会跟随系统的圆角/形状）——安装后桌面即显示，无需手动设置。

界面为**四个页面**，用底部**悬浮导航栏**切换：运行 / 更新 / 插件 / 设置。整体是深色渐变背景，卡片和导航栏都是**液态玻璃质感**：半透明底 + 顶部光泽带 + 底部反光 + 上亮下暗的渐变「折射」描边，悬浮栏上还有一抹**随手机倾斜流动的高光**（加速度计驱动，退到后台自动停）。导航栏背后是真实的内容模糊（**Android 12 及以上**：硬件 `RenderNode` + `RenderEffect`，全分辨率、GPU 模糊；**更低版本**：先缩小位图再做软件模糊），选中项用主题色玻璃胶囊高亮。滚动页面时模糊会**实时跟随**（节流 80 毫秒），页面静止时**每秒**刷新一次背景。

### 页面一「运行」

- **服务器状态**：圆点 + 运行时长。
- **局域网联机地址**：形如 **Java `192.168.1.7:25565`** 和 **基岩 `192.168.1.7:19132`**。
- **数据目录**、**启动 / 停止**按钮、**电池优化**按钮、**复制地址**按钮。
- **控制台**：实时日志 + 命令输入框，支持 `list`、`op 玩家名`、`save-all`、`stop` 等命令。

### 页面二「更新」

- 当前版本号、已安装版本数量与磁盘占用。
- **检查更新**按钮、**下载或更新**按钮（带进度条）、**切换或回滚**按钮、**删除版本**按钮。
- **下载自动重试**（最多 3 次）；下载完成后、安装前会**校验文件确为真正的 aarch64 可执行文件**（检查 ELF 头与机器码 `0xB7`），如果拿到的是错误页面或不完整文件，会直接**报错**而不是装上去。
- 页面下方有**说明卡片**，解释数据存放位置与保留策略。

### 页面三「插件」

插件是**独立的代码**：编译成 dex，运行时加载。它是给「本该属于第三方的小功能」留的位置，
而不是所有功能都塞进主程序。App 自带一个插件商店，**装插件不需要 Root**，
也不需要自己去放文件。

页面分**两段**，顶部切换：

**「已安装」段**（默认落在这里 —— 装好之后 99% 的操作都在这一段）

- 已装插件的列表：名字、版本、说明，以及 **停用 / 启用** 和 **卸载** 两个按钮。
- **插件设置**：插件自己声明的设置项，控件由 App 画（插件不含任何界面代码，
  所以体积只有几 KB）。改完立刻生效。
- **诊断**：插件目录路径、**加载日志**（插件为什么没生效就看这里）、「重新加载插件」按钮。

**「商店」段**

- 点「刷新」拉取可用插件列表；每一项显示名字、版本、说明，以及 **安装 / 更新到 x.x** 按钮。
- 已经装过的会显示「已安装（点一下重装）」。

**几个行为上的约定**

- **停用 ≠ 还原**：停用只是不再加载它的代码，**它帮你调好的设置仍然生效** ——
  已经调好的东西不该被悄悄改回去。要连设置一起清掉就**卸载**。
- **卸载**会先问一次，然后删掉插件代码，并把它写过的设置**一并还原**。
- 插件能影响的只有 App **明确规定可以覆盖**的那几项，改不了系统行为。
  它跑在 App 自己的权限里，拿不到额外权限，也**不会**让 App 变得不安全。
- 商店里的插件就是本仓库 Release 里的附件，信任级别和 App 自身更新一样。

目前商店里有两个示例插件：

| 插件 | 做什么 |
|---|---|
| **控制台字号** | 把运行页控制台的日志文字按 0.5~3 倍缩放（内置字号在手机上偏小） |
| **停止前确认** | 点「停止」时先弹一次确认，避免误触把游戏里的玩家踢下线；也可以关掉它恢复一点即停 |

### 页面四「设置」

- **启动方式切换**：普通模式 / Root 模式。
- **外观**：**明暗**与**颜色**是两条独立的轴，可以自由组合。
  - 明暗三选一：深色 / 浅色 / **自动**（跟随系统的深色模式设置）。默认深色。
  - 颜色七选一：星夜蓝（默认）、松林绿、落日渐晖、樱雾粉、深海青、深紫暮色、石墨灰。
    每套都带深色版和亮色版，换颜色不会顺带改明暗。
  - 切换后背景渐变、卡片底色、按钮、底部玻璃栏、**系统栏图标颜色**会一起跟着变，选择记在本地。
- **下载源设置**：GitHub API 地址、仓库 owner/repo、下载镜像前缀三个输入框 + 保存按钮。下载源在此**显式可改**，不再需要长按。手机直连 GitHub 不通时，把 API 地址换成能返回同样 JSON 的镜像，并把下载镜像前缀填成例如 `https://ghfast.top/`（会拼在 `https://github.com/...` 前面）。
- **清除数据**：清服务端版本 / 清游戏数据 / 全部清空三种粒度。
- **关于**：南瓜坞版本号、检查更新（更新 App 自身）、电池优化设置、复制数据目录路径。

首次使用按顺序操作：

1. 到「更新」页**检查更新**——读取 GitHub Releases，找出 `pumpkin-android-arm64-<日期>` 附件。
2. **下载最新版本**——自动重试最多 3 次，完成后校验 ELF 头与机器码正确才进入安装，下载到应用内部私有目录 `versions/<标签名>/pumpkin`。
3. **启动**——直接运行下载好的服务端。

- 服务以**前台服务**运行，切后台或锁屏也能继续（通知栏有常驻通知）。
- 界面会直接显示联机地址，例如 **Java `192.168.1.7:25565`** 和 **基岩 `192.168.1.7:19132`**。

---

## 三、两种启动模式

界面上有「**启动方式**」按钮切换，两种模式：

### 普通模式（默认）

- App 直接启动下载到**私有目录**的服务端二进制。之所以能执行，是因为 `targetSdk<=28` 的应用被放进 `untrusted_app_27` 这个 SELinux 域，该域**允许对应用私有目录里的文件执行 execve**；`targetSdk>=29` 会被拒绝（即所谓 **W^X 限制**）。这是默认且最省事的方式。

### Root 模式

- 用 `su -c` 命令启动，进程落在 **magisk/su 域**，该域本身不受私有目录执行限制。
- **完全不改动 SELinux**：保持 Enforcing、不改策略文件，所以不会被那些检测 `getenforce` 或 SELinux 状态的 App 发现。
- 首次使用会弹 **Magisk 授权框**。
- 额外价值：**如果将来 Android 取消旧 targetSdk 的豁免，Root 模式仍然可用**。

| 对比项 | 普通模式 | Root 模式 |
|---|---|---|
| 是否需要 Root | 否 | 是（Magisk） |
| 原理 | 靠 targetSdk≤28 的域豁免 | 进程在 magisk/su 域，不受私有目录执行限制 |
| 是否改 SELinux | 不改（本就豁免） | 不改（保持 Enforcing、不改策略） |
| 授权弹窗 | 无 | 首次有 Magisk 授权框 |
| 抗性 | 依赖旧 targetSdk 豁免 | 豁免取消后仍可用 |

---

## 四、数据目录与文件管理

数据分**两处**：

- **服务端程序（各版本）**：应用内部私有目录 `versions/<标签名>/`。**必须如此**，否则无法对该目录执行 execve。普通用户无法直接访问，管理就走 App 内功能。
- **游戏数据**：外部目录 `/sdcard/Android/data/com.pumpkin.server/files/server/`，内含 `config/`、`world/`、`logs/`。

游戏数据可以这样管理：

- 用 **USB 连接电脑**，或手机自带的「文件」App 进入 `Android/data/com.pumpkin.server/files/server/`；
- 改造配置、放入现成的世界存档，都在这里。

---

## 五、版本管理与回滚

- 当前版本之外**最多保留 3 个历史备份**。
- **切换版本**可以回滚到之前的备份；**检查更新 + 下载最新版本**就是升级。
- 注意：切换版本前**要先停止服务端**（会提示），否则文件占用可能失败。

---

## 六、清理数据

「清理数据」按钮有三种粒度，**先看清会删什么再点**：

| 选项 | 清掉的 | 保留的 |
|---|---|---|
| 清理服务端版本 | 下载的服务端程序 | 游戏数据 |
| 清理游戏数据 | `config/ world/ logs/` | 服务端版本 |
| 全部清理 | 两者都删 | 无 |

---

## 七、联机与性能调优

- 同一局域网内，Java 版连 `手机IP:25565`，基岩版连 `手机IP:19132`（地址直接在界面上显示）。
- 性能通用的建议（不逐项照搬可能过时的字段名，**以 App 实际生成的配置文件为准**）：
  - 调小**视距**、调小**模拟距离**，大幅降低区块生成与实体模拟开销，省内存省电；
  - 控制**玩家人数**，手机不适合承载高并发；
  - 必要时用**离线模式**（`online_mode=false`），绕开联网认证。
- 长时间满载会发热降频、吞吐下降，建议按手机内存（建议 ≥4GB）平衡以上参数。

---

## 八、常见问题排查

| 现象 | 原因 / 解决 |
|---|---|
| 下载进度不动 / 直连 GitHub 不通 | 手机访问 GitHub 受限（网络被墙/代理限制）。**首选**：在「设置」页把 **API 地址**换成能返回相同 JSON 的镜像，并在「**下载镜像前缀**」填如 `https://ghfast.top/`（它会拼到 `https://github.com/...` 前面）；也可换网络（手机/电脑热点）或用电脑下好后手动放入对应位置 |
| 下载到一半失败或文件不对 | 下载会**自动重试最多 3 次**，且安装前**校验 ELF 头与机器码 `0xB7`**，错误页面/不完整文件会直接报错，不会误装 |
| 启动后日志出现 `Permission denied` | 普通模式的域豁免未生效（或设备情况特殊）。**改用 Root 模式**启动即可 |
| 进程被系统杀掉 | 手机开了**电池优化**。到系统设置把本 App 设为「不受限制」，并尽量保持在前台 |
| 切换版本提示要先停止 | 服务端仍占用文件。先停止服务端再切换版本 |
| 清理会删什么 | 见「清理数据」表——三种粒度各不相同，**点之前确认** |
| 界面模糊异常 / 想报界面问题 | 到「设置 → 关于」复制**「诊断」**那一行（含 Android API 级别、模糊路径 `RenderNode/Bitmap`、模糊层尺寸、内容区尺寸），原样发出来即可 |

---

## 九、备选方式：Termux

- 旧文档里的 **Termux 方式仍然有效**，作为备选保留：用 F-Droid 版 Termux，把 `pumpkin-android-arm64-<日期>` 二进制放到 Termux 私有目录（`~/`），对 `/sdcard` 或 `~/storage`（noexec 挂载）一律不行。
- 运行前 `termux-wake-lock`、关电池优化，尽量在前台会话运行。
- **原理与南瓜坞一样**：Termux 能运行二进制，靠的同样是 `targetSdk<=28` 应用的 SELinux 域豁免，**不是 Termux 有特权**。Android 10+ 上普通模式能执行私有目录程序，和 Termux 是同一机制。

---

## 十、原理说明

- Android 10+ 只有 **`targetSdk<=28`** 的应用会被放进 `untrusted_app_27` SELinux 域，该域**允许对应用私有目录执行 execve**；`targetSdk>=29` 落在更受限的域，被 W^X 限制，不能从私有目录执行程序。
- 南瓜坞因此把下载的服务端放在**私有目录**执行（普通模式即此机制）。
- Root 模式进程落在 magisk/su 域，**不触碰 SELinux 策略**，故 `getenforce` 等检测仍显示 Enforcing，不会被发现；并且未来即使取消旧 targetSdk 豁免，Root 模式也依然可用。
- Release 保留策略：**<1 个月全部保留；1–2 个月每周保留最后一个；2–12 个月每月保留最后一个；>1 年删除**。

---

## 附：界面之外的说明

App 界面保持简洁，只留必要提示；下面这些原本写在界面里、现已移到文档的技术说明，需要时再查。

### 服务端程序为什么必须放在应用内部私有目录

Android 10 起禁止应用从自己的可写目录执行文件（W^X）。本 App 把 targetSdk 设为 28，运行在 `untrusted_app_27` 域，该域允许对应用私有目录里的文件执行 execve，所以服务端二进制放在 `/data/data/com.pumpkin.server/files/versions/` 下。外部存储（/sdcard）是 noexec 挂载，即使降低 targetSdk 也无法执行，因此二进制不能放那里。

### 游戏数据为什么放在外部目录

世界存档、配置和日志放在 `/sdcard/Android/data/com.pumpkin.server/files/server/`，插 USB 或用系统文件管理器就能访问，方便改配置、放存档。程序与数据分开放，清理时可以各清各的。

### 关于历史版本

最多保留 3 个历史版本用于回滚，下载新版本时最旧的会自动清理。更新会替换服务端程序文件，所以需要先停止正在运行的服务器（App 会主动提示）。

### 下载源与自动回退

点下载后，会依次尝试：上次成功过的源 → 你手动填的镜像前缀 → 直连 github.com → `https://ghfast.top/` → `https://gh-proxy.com/` → `https://ghproxy.net/`。每个源失败会重试 2 次再换下一个，界面上会显示"这个下载源不可用，正在自动换源"。

某个源一旦成功就会被记住，下次优先使用，所以正常情况下完全不需要手动配置镜像；设置页里的输入框只是手动覆盖用。

下载完成后会校验文件是否为真正的 aarch64 可执行文件（ELF 头 + 机器码 0xB7），拿到错误页面或文件不完整时直接报错，不会误装。

### 版本选择

点「检查更新」会列出全部可用版本，**默认选中最新版**。想装旧版就点「选择版本」，列表里会标出「● 当前运行」和「[已下载]」。下载按钮文字会跟随选中的版本变化。

### 沉浸式界面

App 采用 edge-to-edge：状态栏和手势条那一条**不是系统黑边，而是 App 自己的背景渐变**，
所以看起来是「内容铺满整屏、系统栏浮在内容之上」。

实现分工（这里踩过坑，写下来免得再犯）：

- 背景渐变铺满整屏，**包括系统栏底下**。
- 该避让的内容由 Compose 自己算内边距：页面读 `WindowInsets.statusBars`，
  底部玻璃栏读 `navigationBars`。
- **不要**给 `android.R.id.content` 加 `setPadding` 来避让系统栏。那样 padding 出来的
  那一块没有 Compose 内容覆盖，露出的是窗口底色 —— 屏幕上就是上下两条灰带，沉浸感全没了。
  曾经就是这么错的（那两条灰带的颜色是 `Theme.Material` 默认的 `#303030`）。
- 窗口底色（`res/values/themes.xml` 的 `windowBackground`）必须跟背景渐变的顶色一致，
  否则冷启动会先闪一下那个颜色。换配色时窗口底色也跟着换。
- 底栏的模糊采样背板要**整屏**录，内边距加在里层；把背板本身缩小会让采样坐标错位、模糊发飘。