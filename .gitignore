
# miuix 官方参考源码（仅本地查阅，不入库）
.miuix-ref/

# 提交信息临时文件（用 -F 传长消息时产生；别提交进来）
.commitmsg*.txt
.commit-msg*.txt

# 构建产物与本地归档
android-app/build/
android-app/app/build/
android-app/.gradle/

# 本机构建的产物（tools/build-local.ps1 输出：APK + 插件 dex + 商店索引）
out/

# 提交信息临时文件（用 -F 传长消息时产生；别提交进来）
.git-msg.txt

# 工具自己下载到工作区根目录的文档（如 ripwire）。
# 它们不属于本项目，只是恰好落在这里 —— 别被 git add -A 扫进去。
__ripwire*
